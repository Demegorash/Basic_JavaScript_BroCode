// a while loop is to repeat some code while some condition is true

/*
let userName = "";

while (userName === "" || userName === null) {
    userName = window.prompt("Enter your name");
}

console.log(`Hello ${userName}`);

// do 

let userName;

do {
    userName = window.prompt("Enter your name");
} while((userName === "" || userName === null));

console.log(`Hello ${userName}`);

// while

let loggedIn = false;
let username;
let password;

while(!loggedIn) {
    username = window.prompt("Enter your username");
    password = window.prompt("Enter your password");

    if(username === "myUsername" && password === "myPassword") {
        loggedIn = true;
        console.log("You are logged in");
    }
    else {
        console.log("Invalid Credentials, please try it again!");
    }
}

// do

let loggedIn = true;
let username;
let password;

do {
    username = window.prompt("Enter your username");
    password = window.prompt("Enter your password");

    if(username === "myUsername" && password === "myPassword") {
        loggedIn = true;
        console.log("You are logged in");
    }
    else {
        console.log("Invalid Credentials, please try it again!");
    }
}while(!loggedIn)
*/