//  Logical operators are to combine or manipulate boolean values


// AND = &&, OR = || ,  NOT = !

/*
const temp = 20;

// AND = &&

if(temp > 0 && temp <= 30) {
    console.log("The weather is Good!");
}
else {
    console.log("The weather is Bad!");
}

// OR = ||

if(temp <= 0 || temp >= 30) {
    console.log("The weather is Bad!");
}
else {
    console.log("The weather is Good!");
}
*/

// NOT = !

const isSunny = true;

if(!isSunny) {
    console.log("It is Cloudy!");
}
else {
    console.log("It is Sunny!");
}